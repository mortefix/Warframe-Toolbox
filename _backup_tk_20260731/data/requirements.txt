requests
websocket-client

# --- the Qt front end (the default since the PySide6 migration) -------------
# Essentials is the toolkit; Addons carries QtWebEngine, which the three
# embedded web apps run on. Addons is much the larger of the two - it is a
# Chromium build - but it is also what lets the web tabs work on Linux, which
# Edge WebView2 never could.
PySide6-Essentials
PySide6-Addons

# --- the legacy Tk front end (`--tk`) ---------------------------------------
# Only wf_market_helper.py needs this, and only for ITS embedded web apps.
# Nothing under ui/ imports it; it goes the moment that file does.
# pythonnet is not listed separately: pywebview's edgechromium backend pulls it
# in on Windows, and core/webhost.py imports System / Microsoft.Web.WebView2.Core
# through it directly (function-local imports, so a missing CLR degrades rather
# than crashing at startup).
pywebview        # Tk only - embedded web apps via Edge WebView2
